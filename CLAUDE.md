# AttuBot / Doom Bot

Read `notes/agents.md` before starting any task. It contains the full architecture, coding conventions, personality rules, and pitfalls. Additional docs are in `notes/` (style guides in `notes/style/`, feature specs in `notes/features/`, dev guides in `notes/dev/`).

## rules

1. do not edit the rules.
1. do not perform any interactions with Discord without asking.
1. do not create commits without being explicitly asked to.
1. all noqa comments must include a valid reason.
