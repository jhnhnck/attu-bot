# casino.toml — dev config example
# Copy to .secrets/casino.toml and fill in real values before running.

[bot]
token = "YOUR_DISCORD_BOT_TOKEN_HERE"

[mongo]
uri = "mongodb://ferret:27017"
name = "casino"

[features]
enabled = []
